# personal-website

Static retro personal website with:

- a 90s-style boot-up screen
- a small desktop-like landing page
- a central window containing profile, experience, and interests

## Run

Open `index.html` directly in a browser, or serve the folder locally:

```bash
python3 -m http.server 8000
```
